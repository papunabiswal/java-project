pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                git credentialsId: 'GitHub', url: 'https://github.com/papunabiswal/java-project.git'
            }
        }
    }
}